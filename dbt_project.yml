name: finance_portfolio
version: "1.0"
config-version: 2
profile: finance_portfolio
model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
vars:
  calendar_start_date: "2024-01-01"
models:
  finance_portfolio:
    marts:
      +materialized: table
      +schema: mart
